# ==============================================================================
# NEXTSTACK SAAS STARTER - ENVIRONMENT VARIABLES TEMPLATE
# ==============================================================================
# This file documents ALL environment variables used in the application.
# Copy the appropriate .env.example file for your environment:
#   - .env.example (base template)
#   - .env.local.example (local development)
#   - .env.production.example (production)
#   - .env.qa.example (QA/staging)
#
# NEVER commit .env files with real values to version control!
# ==============================================================================

# ==============================================================================
# AUTHENTICATION (Required)
# ==============================================================================
# Clerk Authentication - Get from https://dashboard.clerk.com
# 1. Create a new application
# 2. Copy the API keys from the dashboard

# Public key (safe to expose in client-side code)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_your_publishable_key_here

# Secret key (keep this secret, server-side only)
CLERK_SECRET_KEY=sk_test_your_secret_key_here

# Authentication routing (customize as needed)
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/dashboard
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/dashboard

# ==============================================================================
# DATABASE (Required)
# ==============================================================================
# PostgreSQL connection string
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=SCHEMA

# For local development with Docker:
DATABASE_URL=postgresql://appuser:apppass@localhost:5432/appdb

# For production (use connection pooling):
# DATABASE_URL=postgresql://user:password@prod-host:5432/prod-db?connection_limit=10

# Docker PostgreSQL Configuration (for local development)
POSTGRES_USER=appuser
POSTGRES_PASSWORD=apppass
POSTGRES_DB=appdb
PG_CONTAINER_NAME=nextstack_postgres
PGDATA=/var/lib/postgresql/data

# ==============================================================================
# AWS (Required for S3 Features)
# ==============================================================================
# AWS credentials - Get from AWS IAM console
# Best practice: Use IAM roles when deploying to AWS (EC2, ECS, Lambda)
# For local development: Create IAM user with S3 permissions

AWS_REGION=us-east-1
AWS_DEFAULT_REGION=us-east-1

# IAM credentials (not needed if using IAM roles)
AWS_ACCESS_KEY_ID=<your-aws-access-key-id>
AWS_SECRET_ACCESS_KEY=<your-aws-secret-access-key>

# S3 bucket for file uploads
# Create bucket in S3 console, ensure proper CORS configuration
S3_ASSETS_BUCKET=<your-app-name>-<environment>-assets

# ==============================================================================
# ANALYTICS (Optional)
# ==============================================================================
# Google Analytics 4 - Get from https://analytics.google.com
NEXT_PUBLIC_GA_TAG=G-XXXXXXXXXX

# Microsoft Clarity - Get from https://clarity.microsoft.com
NEXT_PUBLIC_MI_ID=<your-clarity-id>

# ==============================================================================
# APPLICATION (Optional)
# ==============================================================================
# Base URL for the application (used for absolute URLs)
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Node environment
NODE_ENV=development

# ==============================================================================
# NOTES
# ==============================================================================
# Security Best Practices:
# 1. Never commit .env files to version control
# 2. Use environment variables or secrets management in production
# 3. Rotate credentials regularly (every 90 days)
# 4. Use IAM roles instead of access keys when possible
# 5. Enable MFA on all admin accounts
#
# Getting Started:
# 1. Copy .env.example to .env
# 2. Fill in your actual values
# 3. Never commit .env to git
# 4. Use .env.local for local overrides
#
# Production Deployment:
# 1. Use your platform's secrets management (Vercel, Railway, AWS Secrets Manager)
# 2. Never store production credentials in .env files
# 3. Use IAM roles for AWS services
# 4. Enable audit logging for credential access
# ==============================================================================
