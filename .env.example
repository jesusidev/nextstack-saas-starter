# When adding additional environment variables, the schema in "/src/env.mjs"
# should be updated accordingly.

# Prisma
# https://www.prisma.io/docs/reference/database-reference/connection-urls#env
# For local development with Docker: postgresql://appuser:apppass@localhost:5432/appdb
DATABASE_URL="postgresql://appuser:apppass@localhost:5432/appdb"

# Clerk Authentication
# Get your keys from https://dashboard.clerk.com
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_your_publishable_key_here
CLERK_SECRET_KEY=sk_test_your_secret_key_here
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/dashboard
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/dashboard

# Docker local postgres - run `docker-compose up -d`
PG_CONTAINER_NAME='postgres_app'
POSTGRES_USER='appuser'
POSTGRES_PASSWORD='apppass'
POSTGRES_DB='appdb'
PGDATA='/data/postgres-app'

# Analytics (Optional)
# Microsoft Clarity - Get from https://clarity.microsoft.com
NEXT_PUBLIC_MI_ID=<your-clarity-id>
# Google Analytics - Get from https://analytics.google.com
NEXT_PUBLIC_GA_TAG=G-XXXXXXXXXX

# AWS S3 for file uploads
# Create an S3 bucket and IAM user with S3 permissions
S3_ASSETS_BUCKET=<your-app-name>-dev-assets
AWS_REGION=us-east-1
AWS_DEFAULT_REGION=us-east-1
# IAM user credentials (or use IAM roles when deploying to AWS)
# Create IAM user with S3 read/write/delete permissions on your bucket
AWS_ACCESS_KEY_ID=<your-aws-access-key-id>
AWS_SECRET_ACCESS_KEY=<your-aws-secret-access-key>
